<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.findmypackage">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:label="@string/app_name"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FindMyPackage"
        android:screenOrientation="portrait"
        >

        <activity
            android:name=".ui.main.MainActivity"
            android:launchMode="singleTask" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name="com.example.findmypackage.ui.setting.SettingActivity"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTop" />

        <activity
            android:name="com.example.findmypackage.ui.track.add.TrackAddActivity"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTop" />

        <activity
            android:name="com.example.findmypackage.ui.track.detail.TrackDetailActivity"
            android:parentActivityName=".ui.main.MainActivity"
            android:windowSoftInputMode="adjustNothing"
            android:launchMode="singleTop" />

        <service
            android:name="com.example.findmypackage.util.MyNotificationListenerService"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService"/>
            </intent-filter>
        </service>

    </application>

</manifest>